using UnityEngine;

public class VisibleObjectData
{
    public Vector3 position;
    public Vector3 velocity;

    public VisibleObjectData(Vector3 position, Vector3 velocity)
    {
        this.position = position;
        this.velocity = velocity;
    }

}
